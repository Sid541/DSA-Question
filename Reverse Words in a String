class Solution {
    public String reverseWords(String s) {
        String[] arr = s.trim().split("\\s+");
        String str = "";
        for (int i=arr.length-1; i>0; i--) {
            
            str += arr[i] + " ";
        }
        return str + arr[0];
        

    }
}
